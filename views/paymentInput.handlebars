
<div class="row">
    <div>
        <form action="/payment/output" method="post" class="form-centered">
            <div class="standard-input">
                <label>Aluno :</label>
                <select name="studentId">
                    <option></option>
                    {{# each listNames}}
                        <option value={{studentId}}>{{studentName}}</option>
                    {{/each}}
                </select>
            </div>
            
            <div>
                <input type="submit" value="Buscar">
            </div>
        </form> 
    </div>
</div>

